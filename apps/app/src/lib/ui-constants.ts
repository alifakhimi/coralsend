/**
 * UI-layer constants. Use these instead of magic numbers/strings so
 * copy feedback, timeouts, and shared labels stay consistent across the app.
 */

/** How long to show "Copied" state after copy-to-clipboard (ms). */
export const COPY_FEEDBACK_MS = 2000;

/** Default number of columns for segment groups (e.g. max members: 4). */
export const SEGMENT_GROUP_DEFAULT_COLUMNS = 4;
